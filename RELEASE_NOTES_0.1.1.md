# DUMIK Team Plugin 0.1.1 更新公告

这次更新把 DUMIK 从“能写提示词的技能集合”推进为一套更可验证的视频制作工作流。插件包内部版本为 `0.5.22`，GitHub 发布 tag 为 `0.1.1`。

## 这次更新了什么

### 1. 视频拉片从总结升级为证据工作台

`D-video-report` 不再临时拼 HTML。新版使用固定的剪辑台式双栏模板，把原片、A/B 关键帧、镜头时间点、字幕搜索、关键镜头筛选和证据下载放在同一页面。报告数据先经过结构化协议校验，再生成可交互页面，减少时间线错位和重复设计。

### 2. 能拆解真实的复合运镜

旧版常把镜头概括成“推近、横移、环绕”。新版会先识别真实切点，再分别分析摄影机实体位移、观看方向修正、旋转轴、画面旋转方向、推进关系、偏轴漂移、前中后景视差以及非线性速度曲线。推进与旋转同时发生时，会保留为同一条连续轨迹，不再错误拆成两个先后动作。

### 3. 新增深度视频引导模式

新增 `depth-video.md`。黑白 Depth Map 视频只控制空间纵深、姿态、物体位置、遮挡、构图、动作时序和摄影机轨迹；身份图控制人物或产品外观；用户文字控制真实场景、材质、颜色和光线。最终结果不会继承深度图的黑白灰、伪彩、噪点或扫描感。

### 4. Seedance 时长下限统一为4秒

所有 Seedance 提示词总时长不得少于4秒。用户提出1至3秒时，自动改写为4秒：核心动作仍在原指定时间窗内完成，补足时间只用于同一镜头自然减速和稳定停留，不新增动作、不切镜、不循环。时间轴、分镜表、首尾帧说明和最终提示词会同步更新。

### 5. 高级商业电影调度成为正式增强层

`D-video-plan` 与 `D-storyboard` 现在会把抽象的“高级感”拆成可拍摄的决定：可见障碍、摄影机立场、视线流量、物理光色、信息密度和因果结构。电影语言必须增强产品证据和品牌记忆，不能只剩油亮材质、无来源轮廓光、青橙滤镜、烟雾或昂贵布景。

### 6. 故事板连续性和镜号规范更严格

三联、四宫格、九宫格等拼版必须在每格左上角标注 `SHOT 01` 起的两位镜号；独立关键帧和生视频垫图保持干净，不烧录镜号。生产关键帧默认逐镜独立生成再外部拼版，方便单镜返修并避免整板清晰度损失。连续镜头还会锁定时间地点、空间骨架、产品结构、固定道具、光色来源和禁止漂移项。

### 7. 产品身份板和电影化单帧更可靠

`D-storyboard` 明确完整产品实拍、局部参考和材质参考的优先级，禁止用局部图拼想整机，也禁止把失败的 AI 候选反复垫图导致结构劣化。`D-image-prompt` 新增电影化单帧增强，要求摄影机站位、唯一动作、视线落点、物理光源和真实成像基底都能被看见，而不是堆“电影感、高级感”等空词。

## 版本变化

- 插件包：`0.5.18` -> `0.5.22`
- `D-video-report`：`0.1.0` -> `0.3.0`
- `D-video-plan`：`0.1.5` -> `0.1.6`
- `D-storyboard`：`1.1.1` -> `1.3.0`
- `D-image-prompt`：`0.3.10` -> `0.3.11`

## 兼容性

- 旧的图片、故事板和视频提示词入口仍然保留。
- 默认仍然只输出提示词、分镜或报告；没有明确要求生成时，不会调用图片或视频接口。
- 批量生成仍执行“先确认、后批量”的门禁。
- 本次没有加入 API Key、个人路径、项目素材或历史输出。
