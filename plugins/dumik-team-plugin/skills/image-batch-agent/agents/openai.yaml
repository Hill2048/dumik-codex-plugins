interface:
  display_name: "批量图片修改"
  short_description: "项目制批量改图，先出确认图再批量生成"
  default_prompt: "Use $image-batch-agent to set up a batch image editing project, write per-image prompts, generate one confirmation image, then batch generate after approval."
