# DUMIK 图片与视频任务默认提示

完整路由只看：

```text
assets/agent-skill-routing.md
```

本文件只提醒三件事：

- 图片、视频任务先按 AGENTS.md 总监职责给一句判断，再按统一路由分流（总监已并入 AGENTS.md，不再是独立 skill）。
- 默认只交付文字；只有用户明确要求生成、出图、提交或调用接口时，才进入执行 agent。
