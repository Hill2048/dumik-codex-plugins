# 已并入 AGENTS.md

2026-06-11：`ecom-visual-director`（电商视觉总监）和 `tvc-director`（TVC 总导演）不再是独立 skill。

总监职责改为由 `workspace-init` 按项目类型（图片 / 视频 / 混合）写进各工作台的 `AGENTS.md`——入口常驻，不再依赖 skill 触发。职责节模板见 `skills/workspace-init/references/entry-templates.md`。

任务到 skill 的分流以 `assets/agent-skill-routing.md` 为准（总路由已直连底层 skill）。本目录只作历史参考。
