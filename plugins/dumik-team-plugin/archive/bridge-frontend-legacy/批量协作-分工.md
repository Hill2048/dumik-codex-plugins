# 批量协作 · 分工

三方按同一份合同各做各的，互不依赖对方代码，只依赖合同。

**唯一准绳**：`assets/批量协作-文件合同.md`。任何一方都不能单方改字段；要改先同步三方。

## Claude · skills + 桥服务

| 项 | 状态 | 产物 |
|---|---|---|
| 文件合同 | ✅ | `assets/批量协作-文件合同.md` |
| 桥服务骨架 | ✅ | `bridge-server/`（server.js / package.json / README） |
| 编排 skill | ✅ | `skills/ecom-detail-autopilot/SKILL.md` |
| 现有 skill 接合同 | ⬜ | 改 `ecom-detail-planner`（校准回写）、`grid-card-prompts`（run-state/溯源）、`image-batch-agent`（结果回写+qaVerdict）、`product-detail-repair`（阻断式 product-visual-qa） |
| 注册路由 | ⬜ | `agent-skill-routing.md`、`ecom-visual-director`、`README.md`、`skill-versions.json` 登记新 skill |

## Codex · 前端 BatchRefiner

依据：`assets/批量协作-BatchRefiner改造清单.md` + `批量协作-文件合同.md`。原手动能力全保留，只加 Agent 模式。

| 项 | 状态 |
|---|---|
| `mode: manual \| agent` 切换 + Topbar 入口 | ⬜ |
| `src/lib/bridgeClient.ts`：WS 订阅 run-state + POST selection | ⬜ |
| Agent 模式只读同步（task 只读，只剩选片/标注可写） | ⬜ |
| 进度看板（复用 GenerationLogSession + pipeline.stage + stats） | ⬜ |
| 闸① 方向确认 UI（写 directionDecision + annotations） | ⬜ |
| 闸③ 终审 UI（写 finalReview） | ⬜ |
| 选片细化到结果图级 `selectedResultId` | ⬜ |

## Hill · 集成联调

| 项 |
|---|
| 跑桥服务（配 PROJECT_DIR / FRONTEND_DIST / AGENT_API_KEY） |
| build 前端、切 Agent 模式连桥 |
| 验收三个闸的交互、看图、选片、校准是否回写生效 |

## 接口边界

```text
Claude(skills)  ──写 run-state.json / 校准沉淀.json / 图────┐
                                                          ├─ 文件合同
Codex(前端)     ──写 selection.json───────────────────────┘
桥服务(Claude)  ── watch + WebSocket + 收 selection + 托管前端 + 持 Key
```

谁都只对着合同写/读。Codex 不需要看 skills 代码，Claude 不需要看前端代码。合同不变，两边可并行。

## 当前进度

Claude 侧：合同、桥服务、编排 skill 已完成；剩「现有 skill 接合同 + 注册路由」。
Codex 侧：未开始，可凭《改造清单》+《文件合同》直接动手。
